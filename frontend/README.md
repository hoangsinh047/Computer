# NOVA RIG — Premium PC Parts (React + Tailwind)

User-facing e-commerce UI (no backend) for computer accessories & components.

## Tech
- React + TypeScript (Vite)
- Tailwind CSS v4 via `@tailwindcss/vite`
- Mock product data (local TS file)

## Run
```powershell
cd E:\Project\ComputerProject\frontend
npm install
npm run dev
```

## Notes
- Dark premium theme with cyan/indigo gradient accents.
- Components are split by concern: layout / sections / catalog.
- Filtering is fully client-side (query, categories, brand, price, rating, in-stock).

